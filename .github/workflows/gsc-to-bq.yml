name: GSC to BigQuery - Incremental Daily Sync

on:
  workflow_dispatch:  # اجرای دستی
  schedule:
    - cron: "0 3 * * *"  # هر روز ساعت 03:00 UTC (6:30 صبح ایران)

jobs:
  sync-gsc-to-bq:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 🔧 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install google-cloud-bigquery google-auth google-auth-oauthlib google-auth-httplib2 google-api-python-client pandas pyarrow db-dtypes

      - name: 🔑 Write GCP credentials
        run: |
          if [ -n "${{ secrets.BAMTABRIDSAZAN_GCP_SA_KEY_BASE64 }}" ]; then
            echo "${{ secrets.BAMTABRIDSAZAN_GCP_SA_KEY_BASE64 }}" | base64 --decode > gcp-key.json
            echo "✅ WROTE: gcp-key.json from BAMTABRIDSAZAN_GCP_SA_KEY_BASE64"
            echo "gcp-key.json size: $(wc -c < gcp-key.json)"
          else
            echo "❌ ERROR: Missing secret 'BAMTABRIDSAZAN_GCP_SA_KEY_BASE64'"
            exit 1
          fi

      - name: ✅ Validate credentials file
        run: |
          python -c "import json; json.load(open('gcp-key.json')); print('✅ gcp-key.json is valid')"

      - name: 🚀 Running incremental GSC sync...
        run: |
          echo "🚀 Running incremental GSC sync..."
          python gsc_to_bq.py
